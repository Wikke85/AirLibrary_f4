package utils
{
	public class ParsingUtils
	{
		
		/**
		 * format a string to a date, supply an expected format if known, like 'yyyy/mm/dd'
		 * */
		public static function stringToDate(string:String, expectedFormat:String):Date {
			
			throw new Error('This function isn\'t done yet...');
			
			return new Date;
		}
		
		
		/**
		 * format a string to an hour, supply an expected format if known, like 'hh:mm'
		 * */
		public static function stringToHour(string:String, expectedFormat:String):Date {
			
			throw new Error('This function isn\'t done yet...');
			
			return new Date;
		}
		
		
		
	}
}